import React from 'react';
import { Template } from '../../types';

interface TemplateAnalyticsProps {
  template: Template;
}

const TemplateAnalytics: React.FC<TemplateAnalyticsProps> = ({ template }) => {
  return <div>Template Analytics</div>;
};

export default TemplateAnalytics; 